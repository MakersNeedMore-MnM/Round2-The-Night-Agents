# Port Configurations
PORT=5000
FRONTEND_PORT=3000

# Application Environment
NODE_ENV=development

# AI Provider Configuration (Demo Mode: leave blank for zero-config local engine)
# When provided, connects to live LLM / Whisper / Bhashini / Cloud speech APIs
AI_API_KEY=
ASR_ENDPOINT=
TTS_ENDPOINT=

# Security & Sessions
JWT_SECRET=chikitsabodha_clinical_secret_key_2026
SESSION_TIMEOUT_MINUTES=60

# Database Configuration (PostgreSQL URL when running in production)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/chikitsabodha

# ABDM / NDHM Sandbox Gateway
ABDM_CLIENT_ID=
ABDM_CLIENT_SECRET=
ABDM_GATEWAY_URL=https://dev.abdm.gov.in/gateway/v0.5
